# agent-parallel-merge
 A drop-in AGENTS.md that lets parallel AI coding agents land changes into main without GitHub CI/merge stalls — git worktree isolation, strict Worker/Merge Agent role separation, file-based deploy lock.
